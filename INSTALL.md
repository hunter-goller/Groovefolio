# Install and run Groovefolio

The maintained setup instructions are in [docs/development/setup.md](docs/development/setup.md). Start there for Flutter prerequisites, code generation, running on Android, verification, and optional Discogs credentials.

The internal Dart package remains `vinyl_app`; the Android application ID is `app.groovefolio`.
